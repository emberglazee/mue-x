# MUE-X Core (zero dependencies — stdlib only)
# The core agent, CLI, and evolution engine work with Python 3.11+ only.

# Optional: API server (pip install -r requirements.txt)
fastapi>=0.100.0
uvicorn>=0.22.0
